

# 191110 문제풀이

```sql
1. TCP/IP 프로토콜 계층
 - ICMP : Internet Layer
 - SMTP, SNMP, TFTP : Application Layer

2. UDP 패킷 헤더
: Source Port, Destination Port, Length, Checksum

3. TCP
- 상호간의 신뢰성 있는 통신.
    :: 데이터를 패킷으로 나누어 그 안의 일련번호, 주소, 에러검출코드를 다 확인함.
- 흐름 제어 :: 송수신 속도 제어를 위함.
- 에러 검사 -> 재전송. 스스로 수정 XXXXXXXX

4. 홉 카운팅 기반 프로토콜 : `RIP`
- SNMP : IP네트워크 상의 장치로부터 정보를 수집 및 관리.
    정보를 수정하여 장치의 동작을 변경하는 데에 사용되는 인터넷 표준 프로토콜.
- RIP : 경유할 가능성이 있는 라우터를 홉 카운팅하여 인접 호스트와의 경로를 동적으로 교환하는 일.
- SMB : 마더보드에 있는 저속도 장치들과 통신하는데 사용하는 간단한 2선 버스.
- OSPF : 인터넷 프로토콜 네트워크를 위한 링크 스테이트 라우팅 프로토콜.

5. `SSH`
: 22번 포트 사용. 패스워드는 암호화하여 보호됨.
  원격 컴퓨터에 안전하게 액세스 하기 위한 유닉스 기반의 명령 인터페이스 및 프로토콜.

6. `RARP`
- IP를 하드웨어 주소로 바꿔주는 건 ARP.
- 하드웨어 주소를 IP주소로 바꿔주는 건 RARP.

7. `MTU(Maximum Transmission Unit)`
IP 패킷은 네트워크 유형에 따라 전송량에 있어 차이가 나기 때문에 적당한 크기로 분할함.
그때 기준이 되는 것이 -> MTU

8. `255.255.255.224` ?????????????????????????????????????????????????
-> 2진수)11111111.11111111.11111111.11100000
networkID = 1, HostID = 0
16 + 8+ 4+ 2+ 1 = 32개
IPv4의 최대갯수 256 중에 1번은 Gateway, 256번은 Broadcast 사용함으로서 254-224 = 32 -2 = 30;

9. 11101011 
= 128 + 64+ 32+ 0 + 8+ 0 + 2+ 1 = 235;
c 클래스 192~223 / 그러니까 D Class

A : 0       0~127
B : 10      128~191
C : 110     192~223
D : 1100    224~255

10. 서브넷 마스크
: 서브넷 마스크의 Host ID 필디는 0으로 채우고
Network ID는 1로 채워 구분을 할 수 있음.

11. IP 프로토콜
-> OSI 7계층 중 3계층인 네트워크 계층에 속함.
단위는 패킷. 전달하는 역할을 함.

12. `IGMP`
: 멀티캐스트 그룹에 가입한 네트워크 내의 호스트관리 기능을 함.
: G -> Group
- ICMP : 두 호스트 간 통신을 담당하는 프로토콜.

13. [OSI 7계층] PDU(Protocol Data Unit)
- 물리 계층 1 : 비트
- 데이터 계층 2 : 프레임
- 네트워크 계층 3 : 패킷/ 데이터그램
- 전송 계층 4 : 세그먼트
- 5.6.7 계층 : 데이터/ 메세지

14. `SNMP`
: UDP. 비동기식 요청/응답 메시지 프로토콜.
: 4가지 기능(Get, Get Next, Set, Trap)
- SMTP : 단순 이메일 송신할 때 사용하는 TCP 프로토콜.

15. `ICMP(Internet Control Message Protocol)`
: 망 내 교환 장비들이 오류 상황에 대한 보고를 할 수 있게 함.
: 예상하지 못한 상황이 발생한 경우 이를 알릴 수 있도록 지원하는 프로토콜.

16. `tracert`
: 네트워크의 목적지 노드까지 연결 경로를 알기 위해 사용하는 명령어.
: 각 경유지의 응답 속도를 확인할 수 있음.
: 연결 경로도 알 수 있고, 각 경유지의 응답속도도 확인할 수 있어 문제가 발생하면
어디에서 문제가 생긴 것인지 확인할 수 있음.

17. IPv6
-> 애니캐스트 사용.

18. [OSI 7계층]
- 암호/복호, 인증, 압축 -> presentation layer

19. 
- 흐름 제어 : 데이터의 양이나 속도를 제한하는 프로토콜의 기능.

20. 에러제어 기법 - 자동 재전송 기법
- stop and wait arq : 멈추고 기다렸다가 재전송
- go-back n arq : nak신호를 받으면 그 에러가 발생한 블록 이후 모든 데이터를 재전송.
- selective repeat arq : 선택적 재전송, nak신호를 받으면 에러가 발생한 블록만 
                        선택하여 재전송.

- 전진에러 수정(FEC) : 오류검출 및 정정이 가능한 부가코드를 함께 전송.
                    수신 측에서 이용하여 오류검출 및 자제 수정.

21. [OSI 7계층] 
- 전송 계층: 4 / 안정적인 전송로 제공. 데이터 전송시 발생하는 우류 감지 및 복구.
- QOS를 통해 통신 서비스 품질, 지연시간, 데이터 손실률 등을 보장함.

22. 
- 네트워크 계층 (3) :
    IP : 호스트의 주소 결정.
    ICMP : 호스트간 통신 담당.
    ARP : IP -> MAC
- 전송 계층 (4)
    UDP : 비연결형 비신뢰성 프로토콜.

23. 네트워크 구성(Topology)에서 성형(Star)
: 중앙 컴퓨터만 멀쩡하면 됨.

24. Fast Ethernet

25. Optical fiber 광섬유

26. 패킷교환 방식

27. 감쇠 현상

28. [Windows Server 2008 R2] 
netstat 명령어 사용시 프로토콜(TCP or UDP) IP 출력됨.
- NNTP : Application Layer(응용 계층에서 사용하는 프로토콜)

29. [Windows Server 2008 R2] 네트워크 모니터링

30.
31. [Windows Server 2008 R2] `EFS`
-> Encrypting File System
: 허가되지 않은 접근을 보호하고 폴더나 파일을 암호화하는 기능.
32. [Linux 명령어] `du`
-> 사용한 디스크 용량에 대한 정보를 제공.
- pwd : 현재 위치 확인
- cat : 파일 내용 보기
- vi : 파일 내용 수정

33. [Linux 명령어]
- free : 여유, 사용 메모리 양을 보여줌.
- mem : linux에서 사용하는 application의 메모리 양을 보여줌.
- du : 사용한 디스크 용량에 대한 정보 제공.
- cat : 파일의 내용을 간단하게 출력.

34. [Linux 시스템 디렉터리]
- /proc : 가상 파일 시스템
- /var : 시스템 로그와 같이 변화하는 값에 대한 정보 저장.
- /bin : 가장 기본적으로 사용하는 명령어 저장.
- /etc : 각 시스템의 고유한 설정 파일들 위치.

35. 
TTL값이 길수록 (최대 255) 해당 목적지까지 라우터를 적게 통과했다는 뜻.

36. [Windows Server 2008 R2]
원격 데스크톱은 1:1 접속이 원칙.

37. [Windows Server 2008 R2]
- 역방향 조회 : pointer(PTR)
- 정방향 조회 : host(A)

38. 
- allow-update{ none; };
    : DNS를 업데이트할 host를 설정하는 구문.
    이 때 none은 아무도 접근 못하게 하는 설정.

39. [Windows Server 2008 R2] `DHCP 구성`

40. [Linux 명령어]
- mkdir : make directory
- rmdir : remove directory

41. [Windows Server 2008 R2]
- Hyper-V : x64시스템을 위한 하이퍼바이저 기반의 가상화 시스템.
하이퍼 v를 사용하기 위해서는 하드웨어, 스포트웨어들이 가상화 또는 반가상화를
지원해주어야 함. 실행방지(dep)가 기본적으로 필요함.

42. httpd.conf

43. [Linux 명령어] `Shell`
인간의 고급언어를 컴퓨터의 저급언어로 변형해주는 기능
: 커널은 핵, 쉘은 맨틀. 가장 바깐에 있어서 사용자의 명령어를 해석하고 전달함.

44. [Linux]
- GRUB : 리눅스에서 사용하는 부트로더.
- CMOS : BIOS 설정시 읽어오는 내용.
- SWAP : 메모리 부족시 실제 하드를 RAM처럼 사용하는 것을 뜻함.
- MBR : 마스터 부트 레코드. 하드디스크에 대한 정보가 담겨져 있음.

45. [Windows Server 2008 R2] 
FTP, SMTP, NNTP, HTTP/HTTPS(WWW)

46. 로드밸런싱
: 시스템 부하를 피할 수 있는 최적화 기술.

47. [OSI 7계층]
- 1 : repeater, herb
- 2 : switch
- 3 : router

48. `OSPF(Open Shortest Path Fast)`
: 관리자의 허가없이 라우터에 쉽게 접속 ???????큰일남.

49. `RAID`
A 사용하다가 A 용량 다 차면  B 사용. 이런식

50. `클라우드 컴퓨팅`
클라우드 : 인터넷
클라우드 컴퓨팅 : 인터넷을 기반으로 가상화된 컴퓨터의 리소스를 이용해서
자신의 컴퓨터가 아닌 인터넷에 연결된 다른 컴퓨터로 처리하는 기술.

```